<Window x:Class="HashCopier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HashCopier"
        mc:Ignorable="d"
        Title="HashCopier by Jackson Hu" Height="400" Width="530">
    <Grid>
        <Grid Margin="10,10,10,172.5">

            <TextBox HorizontalAlignment="Left" Height="24" Margin="118,18,0,0" TextWrapping="Wrap" Text="Not selected"
                     VerticalAlignment="Top" Width="372" />
            <TextBox HorizontalAlignment="Left" Height="24" Margin="118,56,0,0" TextWrapping="Wrap" Text="Not selected"
                     VerticalAlignment="Top" Width="372" />
            <Button Content="Copy" HorizontalAlignment="Left" Margin="10,90,0,0" VerticalAlignment="Top" Width="228"
                    Height="42" />
            <Button Content="Move" HorizontalAlignment="Left" Margin="243,90,0,0" VerticalAlignment="Top" Width="247"
                    Height="42" />
            <Button Content="Source Path" HorizontalAlignment="Left" Margin="10,17,0,0" VerticalAlignment="Top"
                    Width="103" Height="25" />
            <Button Content="Dest Path" HorizontalAlignment="Left" Margin="10,55,0,0" VerticalAlignment="Top"
                    Width="103" Height="25" />

        </Grid>
        <ListView Margin="10,155,10,55.5" Name="FileList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Source file" Width="350" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Status" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <ProgressBar Margin="10,326,10,29.5" x:Name="SingleFileProgress"/>
        <ProgressBar Margin="10,347,10,8.5" x:Name="WholeProgress"/>

    </Grid>
</Window>